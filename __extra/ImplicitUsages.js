// noinspection JSUnusedGlobalSymbols

/**
 * This should not be called, it only exists so that your editor identifies that these standard methods are actually used:
 * - getTitle
 * - getSubtitle
 * - getDescription
 * - initializeTestRun
 * - onCycleFinished
 */
function __causeImplicitUsage() {
    getTitle();
    getSubtitle();
    getDescription();
    initializeTestRun();
    onCycleFinished();
}
